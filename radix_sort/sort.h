#ifndef SORT_H
#define SORT_H

#include <stdlib.h>
#include <stdio.h>

/* Function prototype for print_array */
void print_array(const int *array, size_t size);

/* Function prototype for radix_sort */
void radix_sort(int *array, size_t size);

#endif /* SORT_H */
